Olá!

Orientações:

    Devido o id de nossas classes serem UUID, será necessário replica-las assim que gerar os seed's para popular o banco de dados,
para que nossas requests no postman funcionem;

    Não conseguimos efetuar sobrecarga no método GET no controller de nossas classes, porém criamos algumas consultas que retornam
valores totais para utilizar em dashboard;

    